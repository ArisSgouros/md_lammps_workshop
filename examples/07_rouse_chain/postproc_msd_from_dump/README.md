# Postprocessing: Rouse-chain MSD using MsdMol

This folder provides an alternative MSD calculation using the external `MsdMol` utility.

It reads `../o.dump.all.lammpstrj` and uses settings from `in_param.dat`.

Run after completing the parent example:

```bash
./run.sh
```
