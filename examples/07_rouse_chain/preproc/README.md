# Preprocessing: Rouse-chain data file

`make_rouse_data.py` generates Gaussian initial conformations for the Brownian Rouse-chain example.

Run from this folder:

```bash
python make_rouse_data.py
```

Then copy or move the generated `in.data` to the parent example folder if needed.
