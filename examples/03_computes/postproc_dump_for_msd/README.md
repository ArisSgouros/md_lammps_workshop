# Postprocessing: MSD from dump

This folder computes the mean-square displacement from `../o.dump.prod.lammpstrj` using the external `MsdMol` utility.

Run after completing the parent example:

```bash
./run.sh
```

Input settings are stored in `in_param.dat`.
