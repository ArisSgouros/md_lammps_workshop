# Preprocessing: command-structure LJ data file

`make_data.py` generates the binary FCC Lennard-Jones data file used by this example.

Run from this folder:

```bash
python make_data.py
```

Then copy or move the generated `in.data` to the parent example folder if needed.
